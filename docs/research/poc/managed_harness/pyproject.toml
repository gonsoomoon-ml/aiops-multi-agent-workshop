[project]
name = "managed-harness-test"
version = "0.1.0"
description = "Smoke test for AgentCore Managed Agent Harness (preview)"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.40.0",
]

[tool.uv]
package = false
